# ITPM-Assignment-01-TestRealWorldSystem

Student ID: IT23670020
Name: Athukorala D.P.R

## Extract

- Save the provided ZIP file and extract it.

## Install packages

From the extracted folder, run:

```
pip install -U pip
pip install playwright openpyxl
playwright install
```

## Run command

From the extracted folder, run:

```
python test_automation.py --excel "Assignment 1 - Test cases-IT23670020.xlsx" --url "https://www.pixelssuite.com/chat-translator" --wait-ms 5000 --type-delay-ms 80 --slow-mo-ms 200 --save-every 1 --keep-open
```
